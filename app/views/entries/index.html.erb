<%= stylesheet_link_tag 'index.css', media: 'all', 'data-turbolinks-track': 'reload' %>

<% for entry in @entries %>
  <h2><%= link_to(entry.title, entry_path(entry)) %></h2>
    <p><%= entry.date %></p>
    <p><%= entry.contents %></p>
<% end %>
<br/>
<p><%= link_to("New entry", new_entry_path) %></p>
<p><%= link_to("Home", root_path) %></p>